{
  "routes": [
    {
      "method": "GET",
      "path": "/skus",
      "handler": "sku.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/skus/count",
      "handler": "sku.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/skus/:id",
      "handler": "sku.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/skus",
      "handler": "sku.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/skus/:id",
      "handler": "sku.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/skus/:id",
      "handler": "sku.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
